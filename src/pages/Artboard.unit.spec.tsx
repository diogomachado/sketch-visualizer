describe("Integration tests in the page Artboard", () => {
  it.todo("should shrunk if the window is smaller than the image");
  it.todo(
    "should to exhibit the original size if the window is bigger than the image"
  );
});

export {};
